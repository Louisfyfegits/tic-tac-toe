package game;


//to be used by game controller to get move when not player ones turn
public class PlayerAI {


    /// needs a get move method to return the chosen move, needs a decide move which decides what move to make depending on diffuclty so maybe diff classes for each AI level or smt
}
